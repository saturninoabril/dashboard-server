# dashboard-server

### Build and Install
Then build and install the binary:

```
$ go install ./...
```

### To spin up the server locally
```
$ make start

// create / migrate schema as needed only
$ make migrate
```
